
Input:
You are an accomplished researcher in machine learning. You are considering a new method described in "New Method" for the research theme provided in "Research Topic". "Related Works" is a list of research papers that are highly relevant to this new method.
Based on the following instructions, output the reasons for the novelty and significance of the newly proposed method, and quantitatively evaluate them.

# Research Topic
LLMのfine-tuningの新しい損失関数の提案

# New Method
{
    "Open Problems": "Existing fine-tuning objectives that use fine-grained signals (e.g. FIGA) rely on surface-form edit distance to decide which tokens are \"good\" or \"bad\".  This string-level heuristic fails when a high-quality revision performs (1) paraphrasing, (2) factual correction that keeps identical surface tokens but changes their order, or (3) replaces an entire clause with semantically equivalent wording.  Consequently, the loss provides no gradient for many genuinely improved but lexically different tokens, limiting alignment quality and hurting generalisation.  We need a token/phrase–level supervision signal that captures semantics, is inexpensive, and can be plugged into standard cross-entropy training without an RL phase.",
    "Methods": "Semantic-Aware Fine-grained Alignment (SAFARI)\n1. Dataset generation (reuse SPA): keep 〈prompt, Y_bad, Y_good〉 triples.\n2. Token-level semantic matching.\n   a. Pass Y_bad and Y_good through a frozen encoder (e5-large-v2).  Obtain contextual embeddings E_bad, E_good (shape T×d).\n   b. For every token t in Y_good, find its most similar token in Y_bad via cosine similarity; similarity<τ (τ=0.7) → token considered NEW-GOOD.  Likewise, tokens in Y_bad whose best match in Y_good has similarity<τ are NEW-BAD.\n   c. Produce weight vector w (length max(T_good,T_bad)):  +α for NEW-GOOD, –β for NEW-BAD, 0 otherwise.  α,β initialised to 1,0.5 but become learnable scalar parameters updated by gradient descent (simple but effective meta-learning).\n3. Loss.\n   Let pθ be model probabilities when conditioned on the prompt.\n   Cross-entropy with weights: L_ce = – Σ_t w_t · log pθ(tok_t).\n   Contrastive regulariser: for each NEW-GOOD token g and its nearest neighbour b in Y_bad, maximise margin m on cosine distance of hidden states hθ(g),hθ(b): L_con = Σ_max(0, m – cos(hθ(g),hθ(b))).\n   Total loss L = L_ce + γ·L_con (γ=0.1).\n4. Implementation: modify FIGA’s DataCollator to output (input_ids, labels, weights, pos_pairs) where pos_pairs stores indices (g,b) for contrastive loss.\n5. Differences to prior work: (i) token judgement uses semantic similarity instead of edit ops, (ii) weighting α,β are learned, (iii) adds representation-level contrastive term, still no RL step and no external reward model at train time.",
    "Experimental Setup": "Models: LLaMA-2-7B and Mistral-7B.\nTraining sets: original SPA (17k) and a cleaned 30k ShareGPT subset (to test domain shift).\nBaselines: SFT, FIGA, DPO, DiscoPOP (LRML) using public implementations.\nMetrics:\n• Automatic: GPT-4 reward score, BERTScore(F1) between output and Y_good, Toxicity (Perspective API), TruthfulQA acc, MMLU.\n• Fine-grained effect: measure recall of NEW-GOOD tokens appearing in model output.\n• Human evaluation: 500 prompts, pairwise win-rate vs FIGA.\nAblations: (i) no contrastive term, (ii) fixed vs learnable α,β, (iii) different encoder backbones.\nRobustness: evaluate on paraphrased prompts (ParaGen) and out-of-domain summaries (CNN/DailyMail).",
    "Experimental Code": "# SAFARI core (extends FIGA’s modeling_llama.py)\nclass SAFARILoss(nn.Module):\n    def __init__(self, alpha=1.0, beta=0.5, margin=0.3, gamma=0.1):\n        super().__init__()\n        self.alpha = nn.Parameter(torch.tensor(alpha))\n        self.beta = nn.Parameter(torch.tensor(beta))\n        self.margin = margin\n        self.gamma = gamma\n        self.cos = nn.CosineSimilarity(dim=-1)\n\n    def forward(self, logits, labels, weights, hidden_states, pos_pairs):\n        # logits: B×T×V, labels: B×T, weights: B×T\n        ce = F.cross_entropy(logits.view(-1, logits.size(-1)), labels.view(-1), reduction='none')\n        ce = ce.view_as(labels)\n        ce_weighted = (weights * ce).mean()\n\n        # contrastive term\n        con_losses = []\n        for i,(g_idx,b_idx) in enumerate(pos_pairs):\n            hg = hidden_states[i, g_idx]  # B×H\n            hb = hidden_states[i, b_idx]\n            cos_d = self.cos(hg, hb)\n            con_losses.append(F.relu(self.margin - cos_d))\n        L_con = torch.stack(con_losses).mean() if con_losses else 0.0\n        return ce_weighted + self.gamma * L_con\n\n# Data collator (semantic weights)\nclass SAFARICollator(DataCollatorForSupervisedDataset):\n    def __call__(self, instances):\n        batch = super().__call__(instances)\n        with torch.no_grad():\n            embeds_bad = encoder(batch['bad_text'])  # external encoder, returns T×d\n            embeds_good = encoder(batch['good_text'])\n        weights, pos_pairs = [], []\n        for eb, eg in zip(embeds_bad, embeds_good):\n            sim = cosine_sim_matrix(eg, eb)\n            good_idx = (sim.max(-1).values < tau).nonzero(as_tuple=False).squeeze(-1)\n            bad_idx  = (sim.max(0).values < tau).nonzero(as_tuple=False).squeeze(-1)\n            w = torch.zeros(len(eg))\n            w[good_idx] = 1   # +α (applied inside loss)\n            w[bad_idx]  = -0.5\n            weights.append(w)\n            pos_pairs.append(list(zip(good_idx.tolist(), sim.argmax(-1)[good_idx].tolist())))\n        batch['weights'] = pad_and_stack(weights)\n        batch['pos_pairs'] = pos_pairs\n        return batch\n\n# Integrate with Trainer\nloss_fn = SAFARILoss()\noutputs = model(**input)\nloss = loss_fn(outputs.logits, input['labels'], input['weights'], outputs.hidden_states[-1], input['pos_pairs'])",
    "Expected Result": "1. SAFARI improves GPT-4 reward score over FIGA by ≈+1.0 and over SFT by ≈+4.0.\n2. BERTScore rises by >1.5 points, indicating better semantic alignment.\n3. NEW-GOOD token recall increases from 62% (FIGA) to 78%.\n4. Maintains or slightly improves on bias and toxicity metrics; no regression on MMLU.\n5. Human eval: 57% win rate vs FIGA, 68% vs DPO.\n6. Ablations show contrastive term contributes +0.4 reward, learnable α,β add +0.3.",
    "Expected Conclusion": "SAFARI demonstrates that replacing surface-form edit distance with semantic similarity and learning token weights yields consistently better alignment without RL or extra human labels.  The method is generic, plug-and-play with existing SFT pipelines, and requires only a frozen sentence encoder—no reward model or external critique LLM.  Future work can explore end-to-end joint training of the encoder, multilingual extensions, and integrating SAFARI with low-rank adaptation for resource-efficient deployment."
}

# Related Works
{
    "Title": "Beyond Imitation: Leveraging Fine-grained Quality Signals for Alignment",
    "Main Contributions": "The paper introduces FIGA (Fine-grained Quality Signals for Alignment), an improved LLM alignment approach that addresses the limitations of supervised fine-tuning (SFT) and the complexity of reinforcement learning from human feedback (RLHF). FIGA leverages fine-grained (token or phrase level) quality signals derived by contrasting good and bad responses. Its two main contributions are the curation of a refined alignment dataset (SPA) that pairs initial and revised responses, and a new loss function that uses these fine-grained signals to guide LLM learning. FIGA demonstrates superior performance, outperforming the initial supervised fine-tuned model by 3.2 points and the strong PPO method by 1.8 points, making it the first attempt to use fine-grained quality signals for alignment without RL.",
    "Methodology": "FIGA's methodology involves two key components: a specially curated alignment dataset called SPA and a novel fine-tuning loss function. The SPA dataset is constructed by first generating initial responses (ˆY) from an LLM (e.g., Alpaca-7b) for existing instruction and preference datasets. These responses are then filtered using a reward model to identify queries where the LLM performs suboptimally (RˆY < η1, RY > η2, RY - RˆY > η3). Subsequently, a powerful LLM (e.g., ChatGPT) revises these initial low-quality responses (ˆY) into high-quality pseudo-reference responses (˜Y), aiming for minimal adjustments to reduce distribution shifts. The fine-tuning process utilizes Levenshtein distance to compare ˆY and ˜Y, identifying newly added/substituted (desired) and deleted/substituted (undesired) tokens. Token-level weighting functions, ˜r(·) and ˆr(·), are applied: ˜r(·) rewards desired tokens (α=1) and ˆr(·) penalizes undesired tokens (β=0.5), while common tokens are largely ignored (γ=0). The final loss function encourages the probability of desired words and decreases that of undesired words, effectively acting as a simplified, efficient version of RL without needing a separate critic model.",
    "Experimental Setup": "The initial model used as a rollout model was Alpaca-7b. GPT-3.5-turbo was employed for revising responses to create the SPA dataset. The reward model for filtering the SPA dataset was OpenAssistant/reward-model-deberta-v3-large-v2. The training instance pool comprised diverse datasets including HH-RLHF, ShareGPT, Instruct GPT-J Pairwise, SHP, and OpenOrca. Threshold values for SPA dataset filtering were empirically set to η1=1, η2=3, and η3=3.5, resulting in a SPA dataset of 17,333 instances. FIGA's hyperparameters were set to α=1, β=0.5, and γ=0, with an additional filter for bad tokens (negative log-likelihood < 0.6). Baselines for comparison included SFT, PPO (on SPA and a larger 85K dataset), CoH, RRHF, and DPO. Evaluation was conducted on a test set of 3,608 entries using reward scores, and a suite of out-of-distribution benchmarks: MMLU (knowledge utilization), TruthfulQA, CrowS-Pairs, WinoGender (human alignment), and Vicuna, WizardLM (open-ended generation). Performance was measured by accuracy, bias rate, average scores, and human evaluation (error types, pairwise comparisons).",
    "Limitations": "The paper implies several limitations, though not explicitly listed under a 'Limitations' section. A primary constraint is the reliance on a powerful external LLM (e.g., ChatGPT) for generating revised responses, which entails computational costs and external dependencies. The effectiveness of deriving fine-grained quality signals heavily depends on the Levenshtein distance, which might not fully capture complex semantic or contextual changes in revisions. The weighting coefficients (α, β, γ) are empirically set, potentially limiting optimal performance across different tasks or models, despite some experimental insensitivity to β. Furthermore, the method's dependency on a reward model for initial dataset filtering means that any inaccuracies or biases inherent in the reward model could propagate through the dataset construction.",
    "Future Research Directions": "Future research could explore more sophisticated methods for generating fine-grained quality signals beyond Levenshtein distance, aiming to capture deeper semantic and contextual nuances in revisions. Investigating strategies to reduce the reliance on powerful external LLMs for response revision, possibly through internal iterative self-refinement mechanisms within the model itself, would be beneficial. Developing adaptive or learned approaches for determining the weighting coefficients (α, β, γ) could enhance the model's robustness and general applicability across various scenarios. Additionally, applying the FIGA framework to a wider array of LLM architectures and scales would further validate its scalability and effectiveness, and extending it to incorporate a broader spectrum of human preferences or alignment objectives beyond helpfulness, honesty, and harmlessness could be valuable.",
    "Experiment Code": "class LlamaForCausalLM(LlamaPreTrainedModel):\n    def __init__(self, config):\n        super().__init__(config)\n        self.model = LlamaModel(config)\n\n        self.lm_head = nn.Linear(config.hidden_size, config.vocab_size, bias=False)\n\n        # Initialize weights and apply final processing\n        self.post_init()\n\n    def get_input_embeddings(self):\n        return self.model.embed_tokens\n\n    def set_input_embeddings(self, value):\n        self.model.embed_tokens = value\n\n    def get_output_embeddings(self):\n        return self.lm_head\n\n    def set_output_embeddings(self, new_embeddings):\n        self.lm_head = new_embeddings\n\n    def set_decoder(self, decoder):\n        self.model = decoder\n\n    def get_decoder(self):\n        return self.model\n\n    @add_start_docstrings_to_model_forward(LLAMA_INPUTS_DOCSTRING)\n    @replace_return_docstrings(output_type=CausalLMOutputWithPast, config_class=_CONFIG_FOR_DOC)\n    def forward(\n        self,\n        input_ids: torch.LongTensor = None,\n        attention_mask: Optional[torch.Tensor] = None,\n        position_ids: Optional[torch.LongTensor] = None,\n        past_key_values: Optional[List[torch.FloatTensor]] = None,\n        inputs_embeds: Optional[torch.FloatTensor] = None,\n        labels: Optional[torch.LongTensor] = None,\n        use_cache: Optional[bool] = None,\n        output_attentions: Optional[bool] = None,\n        output_hidden_states: Optional[bool] = None,\n        return_dict: Optional[bool] = None,\n        my_mask: Optional[torch.Tensor] = None,\n        cook_ratio: Optional[float] = 1,\n        cook_ratio2: Optional[float] = -0.5,\n        delta: Optional[torch.Tensor] = None,\n        weights: Optional[torch.Tensor] = None,\n        contra: Optional[bool]=True\n    ) -> Union[Tuple, CausalLMOutputWithPast]:\n        r\"\"\"\n        Args:\n            labels (`torch.LongTensor` of shape `(batch_size, sequence_length)`, *optional*):\n                Labels for computing the masked language modeling loss. Indices should either be in `[0, ...,\n                config.vocab_size]` or -100 (see `input_ids` docstring). Tokens with indices set to `-100` are ignored\n                (masked), the loss is only computed for the tokens with labels in `[0, ..., config.vocab_size]`.\n\n        Returns:\n\n        Example:\n\n        ```python\n        >>> from transformers import AutoTokenizer, LlamaForCausalLM\n\n        >>> model = LlamaForCausalLM.from_pretrained(PATH_TO_CONVERTED_WEIGHTS)\n        >>> tokenizer = AutoTokenizer.from_pretrained(PATH_TO_CONVERTED_TOKENIZER)\n\n        >>> prompt = ",
    "Experiment Result": "data_collator = DataCollatorForSupervisedDataset(tokenizer=tokenizer)\n    return dict(train_dataset=train_dataset, eval_dataset=None, data_collator=data_collator)\n\ndef train():\n    parser = transformers.HfArgumentParser((ModelArguments, DataArguments, TrainingArguments))\n    model_args, data_args, training_args = parser.parse_args_into_dataclasses()\n\n    model = transformers.AutoModelForCausalLM.from_pretrained(\n        model_args.model_name_or_path,\n        cache_dir=training_args.cache_dir,\n    )\n    \n    tokenizer = transformers.AutoTokenizer.from_pretrained(\n        model_args.model_name_or_path,\n        cache_dir=training_args.cache_dir,\n        model_max_length=training_args.model_max_length,\n        padding_side=\"right\",\n        use_fast=False,\n    )\n    \n    if tokenizer.pad_token is None:\n        tokenizer.pad_token = tokenizer.eos_token\n        tokenizer.pad_token_id = tokenizer.eos_token_id\n\n    data_module = make_supervised_data_module(tokenizer=tokenizer, data_args=data_args)\n    \n    trainer = Trainer(model=model, tokenizer=tokenizer, args=training_args, **data_module)\n    trainer.train()\n    trainer.save_state()\n    trainer.save_model(output_dir=training_args.output_dir)\n\n\nif __name__ == \"__main__\":\n    train()\n\ndef transform_tensor(source, target):\n    operations = Levenshtein.editops(source.tolist(), target.tolist())\n    return [i[2] for i in operations if i[0]!='delete'], [i[1] for i in operations if i[0]!='insert']\n\n@dataclass\nclass DataCollatorForSupervisedDataset(object):\n    \"\"\"Collate examples for supervised fine-tuning.\"\"\"\n\n    tokenizer: transformers.PreTrainedTokenizer\n\n    def __call__(self, instances: Sequence[Dict]) -> Dict[str, torch.Tensor]:\n        input_ids = [instance[\"input_ids\"] for instance in instances]\n        raw_input_ids = [instance[\"raw_input_ids\"] for instance in instances]\n        labels = [instance[\"labels\"][0] for instance in instances]\n        raw_labels = [instance[\"labels\"][1] for instance in instances]\n\n        max_len = max(max(len(seq) for seq in input_ids), max(len(seq) for seq in raw_input_ids))\n        input_ids = [F.pad(seq, (0, max_len - len(seq)), value=self.tokenizer.pad_token_id) for seq in input_ids]\n        raw_input_ids = [F.pad(seq, (0, max_len - len(seq)), value=self.tokenizer.pad_token_id) for seq in raw_input_ids]\n        input_ids = torch.stack(input_ids)\n        raw_input_ids = torch.stack(raw_input_ids)\n\n        max_len_labels = max(max(len(seq) for seq in labels), max(len(seq) for seq in raw_labels))\n        my_mask = torch.zeros(input_ids.shape[0], max_len_labels)\n        my_mask_neg = torch.zeros(input_ids.shape[0], max_len_labels)\n\n        # Positive masks\n        for idx, exmp in enumerate(labels):\n            operations, op=transform_tensor(raw_labels[idx], labels[idx])\n            for j in operations:\n                my_mask[idx][j]=1\n            for j, _ in enumerate(labels[idx]):\n                if labels[idx][j]==self.tokenizer.eos_token_id:\n                    my_mask[idx][j]=1\n                    break\n\n        # Negative masks\n        for idx, exmp in enumerate(raw_labels):\n            op, operations = transform_tensor(raw_labels[idx], labels[idx])\n            for j in operations:\n                my_mask_neg[idx][j] = -1\n            for j, _ in enumerate(raw_labels[idx]):\n                if raw_labels[idx][j]==self.tokenizer.eos_token_id:\n                    my_mask_neg[idx][j]=1\n                    break\n        labels = [F.pad(seq, (0, max_len_labels - len(seq)), value=IGNORE_INDEX) for seq in labels]\n        raw_labels = [F.pad(seq, (0, max_len_labels - len(seq)), value=IGNORE_INDEX) for seq in raw_labels]\n        labels = torch.stack(labels)\n        raw_labels = torch.stack(raw_labels)\n  \n        all_input_ids = torch.stack([torch.cat([input_ids[i], raw_input_ids[i]]) for i in range(len(input_ids))]).view(-1, input_ids.size(1))\n        all_labels = torch.stack([torch.cat([labels[i], raw_labels[i]]) for i in range(len(labels))]).view(-1, labels.size(1))\n        all_attention_mask = torch.stack([torch.cat([input_ids.ne(self.tokenizer.pad_token_id)[i], raw_input_ids.ne(self.tokenizer.pad_token_id)[i]]) for i in range(len(input_ids))]).view(-1, input_ids.size(1))\n        all_my_mask = torch.stack([torch.cat([my_mask[i], my_mask_neg[i]]) for i in range(len(my_mask))]).view(-1, my_mask.size(1))\n\n        return dict(\n            input_ids=all_input_ids,\n            labels=all_labels,\n            attention_mask=all_attention_mask,\n            my_mask=all_my_mask,\n        )\n\n\ndef make_supervised_data_module(tokenizer: transformers.PreTrainedTokenizer, data_args) -> Dict:\n    \"\"\"Make dataset and collator for supervised fine-tuning.\"\"\"\n    train_dataset = SupervisedDataset(tokenizer=tokenizer, data_path=data_args.data_path, instruction_type=data_args.instruction_type)\n"
}{
    "Title": "Selecting Large Language Model to Fine-tune via Rectified Scaling Law",
    "Main Contributions": "This paper addresses the challenge of selecting the most appropriate Large Language Model (LLM) for fine-tuning under resource constraints. It formulates this as a problem of predicting fine-tuning performance and establishes a connection to Scaling Law. The key contributions include the discovery of a 'pre-power phase' in fine-tuning scaling curves, preceding the known 'power phase,' especially in low-data regimes. The paper theoretically explains this phenomenon through the concept of 'pre-learned data size' and proposes a 'Rectified Scaling Law' that better fits experimental results. Based on this new law, a novel LLM selection algorithm called 'Accept then Stop' (AtS) is developed, which selects near-optimal models with significantly reduced resource consumption (hundreds of times less) compared to existing intuitive methods that often fail or even provide negatively correlated predictions.",
    "Methodology": "The methodology revolves around understanding and rectifying the Scaling Law for LLM fine-tuning. First, it involves extensive experimentation with 30 diverse LLMs on three datasets across a wide range of data sizes to observe fine-tuning scaling behavior. The paper identifies a 'pre-power phase' where the loss curve's slope gradually decreases before transitioning to the 'power phase' (linear on log-log scale). Theoretically, this phase transition is explained by introducing 'pre-learned data size' (Dl), representing knowledge acquired during pre-training. This concept is integrated into the 'Rectified Scaling Law': L(D) = B / (Dl + D^beta) + E, which is shown to fit experimental results much better than previous power-law forms. For LLM selection, the 'Accept then Stop' (AtS) algorithm is proposed. AtS fine-tunes a model on progressively smaller subsets, collecting loss-size pairs. It fits a linear function to these pairs and stops once a 'stop indicator' (based on deviation from linearity) signals the pre-power phase transition. Finally, it uses the fitted linear function to extrapolate and predict the full-fine-tuning performance.",
    "Experimental Setup": "The experimental setup includes 30 open-sourced LLMs, ranging from 100 million to 7 billion parameters, encompassing various architectures (encoder-decoder like T5, decoder-only like GPT2), pre-trained corpora, and training strategies. Representative models like GPT2, LaMini-GPT, Cerebras-GPT, and mT5 were used. Fine-tuning was conducted on three diverse NLP tasks: machine translation (WMT19 English-Chinese), paragraph summarization (Gigaword), and multi-task instruction tuning (FLAN). To study scaling behavior, subsets with D samples (D ∈ {200, 400, ..., 1,638,400}) were randomly selected for each dataset. Models were fine-tuned three times per setting to mitigate randomness. Optimization used AdamW optimizer and cosine learning rate scheduler, with hyper-parameter search for learning rates and batch sizes to ensure optimal test losses. Evaluation metrics for LLM selection included Pearson correlation coefficient (PearCorr) between predicted scores and true full-fine-tuning performance, and Relative Accuracy (RelAcc) to quantify performance gaps against optimal and worst models. Baselines compared against AtS included ModelSize (using log of parameters), ZeroShot (zero-shot performance), and SubTuning (performance on a small subset). Experiments were primarily run on NVIDIA A100 GPUs using PyTorch and HuggingFace libraries, consuming an estimated 20,000 GPU hours.",
    "Limitations": "The AtS algorithm suffers performance degradation when the data budget ratio (γ) is extremely small, and all observed data points fall within the pre-power phase, making accurate prediction challenging. The paper also notes a lack of a more comprehensive understanding of the intrinsic mechanism behind the pre-power phase and the phase transition itself, including why it often occurs around a test loss value of 2.2. Furthermore, the current study focuses on standard fine-tuning, and the applicability of these Scaling Laws and the AtS algorithm to other fine-tuning strategies like RLHF or LoRA, or under different resource constraints, is not explored.",
    "Future Research Directions": "Future research directions include developing new algorithms that can make accurate predictions even with observations solely from the pre-power phase. Another direction is to investigate whether the benefits of Scaling Laws and the proposed methodology can be extended to other fine-tuning strategies such as RLHF (Reinforcement Learning from Human Feedback) or LoRA (Low-Rank Adaptation), or to address more diverse types of resource constraints. It would also be valuable to conduct further research into the underlying mechanisms of the pre-power phase and the phase transition, exploring if this behavior appears in contexts outside standard fine-tuning and whether its characteristics are consistent. Finally, the paper advocates for collaborative and decentralized research on Scaling Laws to better utilize computational resources and promote sustainable LLM development.",
    "Experiment Code": null,
    "Experiment Result": null
}{
    "Title": "Discovering Preference Optimization Algorithms with and for Large Language Models",
    "Main Contributions": "The paper introduces an LLM-driven objective discovery pipeline to automatically generate novel state-of-the-art offline preference optimization algorithms, overcoming limitations of human-crafted loss functions. It discovers multiple high-performing losses, including Discovered Preference Optimization (DiscoPOP, or Log Ratio Modulated Loss - LRML), a novel algorithm that adaptively blends logistic and exponential losses. DiscoPOP demonstrates strong performance across multi-turn dialogue (MT-Bench, Alpaca Eval 2.0), summarization (TL;DR), and controlled sentiment generation (IMDb) tasks, outperforming existing methods like DPO and SLiC. An initial analysis reveals surprising features of DiscoPOP, such as its non-convex nature.",
    "Methodology": "The core methodology is an LLM-driven objective discovery pipeline. An LLM (GPT-4) is iteratively prompted to propose new preference optimization loss functions in PyTorch code. The process starts with an initial context of established loss functions and their performance. For each proposed objective function, unit tests are run for validity. Valid functions are then used to finetune a Large Language Model (LLM) and evaluated on a predefined downstream validation task (MT-Bench). The resulting performance metric (fitness) is fed back to the LLM as in-context examples, allowing it to iteratively refine its proposals based on past successes and failures. This meta-optimization approach leverages the LLM's knowledge for flexible selection and mutation of objective functions without requiring a pre-designed search space.",
    "Experimental Setup": "The discovery process focused on multi-turn dialogue using a `zephyr-7b-gemma-sft` model (Gemma-7B finetuned on `deita-10k-v0-sft`) and the `Argilla DPO Mix 7K` pairwise preference dataset. All models were trained with fixed hyperparameters (β=0.05, LR=5e-7, bfloat16, 2 epochs, batch size 2/device, gradient accumulation 8, cosine scheduler, AdamW) on 8 Nvidia A100 GPUs. Performance was evaluated using MT-Bench, which uses GPT-4 as a judge for multi-turn open-ended questions. Held-out evaluations were conducted on: 1) Single-turn dialogue using Alpaca Eval 2.0 (win rate against GPT-4 or SFT base model, including length-controlled metrics). 2) Summarization on a 10% subsample of the Reddit TL;DR dataset, evaluated with Alpaca Eval 2.0 using a custom dataset and GPT-4 annotator template. 3) Positive sentiment generation on the IMDb dataset, starting with a GPT-2 model, evaluated using a pre-trained sentiment classifier as a reward scorer and KL-Divergence across a sweep of β values (0.01 to 5).",
    "Limitations": "The current approach has several limitations: limited exploration of LLM objective proposals (e.g., temperature sampling or context sorting did not yield significant improvements); the highest-performing loss (DiscoPOP) re-purposes the β parameter, affecting both its functional behavior and KL penalty. This leads to convergence issues when β is too low (≤0.01) or too high (≥2.5) during training, likely because the discovery process was fixed at β=0.05. High β values can also lead to training instability, model collapse, and NaN outputs. Furthermore, the reliance on closed-source models (GPT-4) for code generation limits reproducibility and incurs high costs.",
    "Future Research Directions": "Future work could focus on more effective methods for generating LLM objective proposals, such as leveraging visual language models (VLMs) by providing entire learning curve plots as context, or meta-meta-optimizing the LLM prompt itself. Another direction is to study objective functions with multiple floating-point parameters that can be tuned separately, rather than re-purposing a single β parameter, which could lead to a more robust and stable training. A multi-parameter analysis and objective reformulation would be beneficial. Finally, research could explore using the discovered models themselves to generate code, moving towards code-level self-improvement in language models, and addressing potential issues like non-convexity with techniques like gradient clipping for stability.",
    "Experiment Code": "fastchat/conversation.py",
    "Experiment Result": "The core methodology is an LLM-driven objective discovery pipeline. An LLM (GPT-4) is iteratively prompted to propose new preference optimization loss functions in PyTorch code. The process starts with an initial context of established loss functions and their performance. For each proposed objective function, unit tests are run for validity. Valid functions are then used to finetune a Large Language Model (LLM) and evaluated on a predefined downstream validation task (MT-Bench). The resulting performance metric (fitness) is fed back to the LLM as in-context examples, allowing it to iteratively refine its proposals based on past successes and failures. This meta-optimization approach leverages the LLM's knowledge for flexible selection and mutation of objective functions without requiring a pre-designed search space."
}{
    "Title": "Beyond Imitation: Leveraging Fine-grained Quality Signals for Alignment",
    "Main Contributions": "Introduces FIGA, a novel LLM alignment approach leveraging fine-grained (token/phrase level) quality signals to overcome limitations of SFT (imitation learning) and RLHF (complexity/instability). FIGA curates the SubPar Alignment (SPA) dataset, pairing initial subpar LLM responses with minimally revised high-quality ones to focus on weaknesses and reduce distribution shift. It devises a new loss function with token-level weighting, derived from Levenshtein distance contrasting responses, to explicitly encourage desired actions and penalize undesired ones. FIGA demonstrates superior performance against competitive baselines (SFT, PPO, CoH, RRHF, DPO) across various benchmarks, achieving better human alignment and task-solving capabilities.",
    "Methodology": "FIGA's methodology involves two main stages. First, the SPA dataset is constructed by: 1) collecting an initial pool of diverse instruction and preference datasets; 2) generating initial responses (ˆY) from a base LLM (Alpaca-7b); 3) filtering instances using a reward model (OpenAssistant/reward-model-deberta-v3-large-v2) to identify subpar initial responses with clear quality differences from human-preferred ones; and 4) revising the subpar responses (ˆY) into high-quality ones (˜Y) using a powerful LLM (gpt-3.5-turbo), making minimal changes based on identified reasons for poor quality (e.g., inaccuracy, lack of detail). Second, fine-grained quality-aware alignment tuning is performed by: 1) using Levenshtein distance to identify token-level additions, deletions, and substitutions between ˆY and ˜Y; 2) applying token-level weighting functions (˜r, ˆr) that assign positive weights (α) to desired tokens (added/substituted in ˜Y) and negative weights (β) to undesired tokens (deleted/substituted in ˆY), while unchanged tokens receive a zero weight (γ); 3) optimizing a new loss function that combines increasing the log-probability of desired words in ˜Y and decreasing the log-probability of undesired words in ˆY, effectively acting as a simplified, efficient version of RL without a separate reward model.",
    "Experimental Setup": "The base model used was Alpaca-7b. The SPA dataset was constructed from an initial pool of HH-RLHF, ShareGPT, Instruct GPT-J Pairwise, SHP, and OpenOrca datasets. Filtering thresholds for reward scores were empirically set at η1=1, η2=3, and η3=3.5. ChatGPT (gpt-3.5-turbo) was used for response revision, and OpenAssistant/reward-model-deberta-v3-large-v2 served as the reward model. The final SPA dataset comprised 17,333 instances. FIGA's hyper-parameters were α=1, β=0.5, and γ=0, with an additional filter retaining bad tokens with negative log-likelihood less than 0.6. Baselines included SFT, PPO (trained on SPA and an extended 85K dataset), CoH, RRHF, and DPO. Evaluation was conducted on a 3,608-entry in-distribution test set using reward scores, and out-of-distribution benchmarks: MMLU (knowledge), TruthfulQA, CrowS-Pairs, WinoGender (human alignment), and Vicuna, WizardLM (open-ended generation, rated by ChatGPT). Human evaluation for error types and overall quality was also performed.",
    "Limitations": "The quality and effectiveness of the revised responses (˜Y) are heavily dependent on the capabilities of the external powerful LLM (e.g., ChatGPT) used for generating these revisions. The reliance on Levenshtein distance for identifying fine-grained 'good' and 'bad' tokens might not always fully capture complex semantic improvements or subtle stylistic nuances. Furthermore, the filtering process for constructing the SPA dataset and initial evaluation of responses depends on an external reward model, which can have inherent inaccuracies, particularly in out-of-distribution scenarios. The weighting coefficients (α, β, γ) for the loss function are empirically set rather than adaptively learned.",
    "Future Research Directions": "Future research could explore more sophisticated methods for deriving and leveraging fine-grained quality signals beyond Levenshtein distance, potentially incorporating deeper semantic understanding. Investigating learned or adaptive mechanisms for setting the token-level weighting coefficients (α, β, γ) instead of empirical determination is another promising avenue. The fine-grained alignment framework could be extended to address more specific alignment challenges, such as bias reduction or improving factuality, or applied to specialized domains. Additionally, research into developing more robust and accurate reward models could further enhance the data filtering and initial response assessment processes within this framework.",
    "Experiment Code": "modeling_llama.py",
    "Experiment Result": "The `modeling_llama.py` file contains the PyTorch implementation of the LLaMA model architecture. This includes classes for LlamaRMSNorm, LlamaRotaryEmbedding, LlamaMLP, LlamaAttention, LlamaDecoderLayer, LlamaPreTrainedModel, LlamaModel, LlamaForCausalLM, and LlamaForSequenceClassification. These components are fundamental for building and training LLaMA-based models. The file also includes utility functions for attention masking and weight initialization. Specifically, `LlamaForCausalLM` is the primary class for causal language modeling tasks, and it incorporates the custom loss calculations for the FIGA method (commented as '# FIGA and corresponding variants changes below:'). The `forward` method in `LlamaForCausalLM` shows conditional logic for calculating the loss based on the presence of `my_mask`, `delta`, and `weights` arguments, which correspond to different weighting strategies mentioned in the FIGA methodology (e.g., delta weighted method, FIGA main method, constant weighted method, chatgpt weighted method). The `train.py` script, which uses this model, also shows how `my_mask` is generated using Levenshtein distance in `DataCollatorForSupervisedDataset`."
}

# Instructions
Following the instructions below, please provide an evaluation of the new method.
Since I aim to pursue research of high academic significance, I request that the assessment be conducted with rigorous standards.
- output
    - novelty_reason
        - Determine whether the new method has novelty, and output the reason.
        - The reason should be as specific as possible.
        - Carefully review the content of the studies provided in "Related Works" before outputting.
    - novelty_score
        - Score the novelty of the new method on a scale of 1 to 10, where 1 means no novelty at all and 10 means extremely high novelty.
    - significance_reason
        - Determine whether the new method is significant, and output the reason.
        - Significance includes both academic and societal importance.
    - significance_score
        - Score the significance of the new method on a scale of 1 to 10, where 1 means no significance at all and 10 means extremely high significance.
Output:
{'novelty_reason': 'The core prior art for fine-grained supervised alignment is FIGA, which decides per-token rewards solely from Levenshtein edits.  SAFARI departs from this in three concrete ways that are absent from the cited works: (1) it replaces string-level matching with encoder-based semantic matching, enabling supervision to survive paraphrase, re-ordering and clause-level rewrites that FIGA’s own authors listed as open problems but did not solve; (2) it makes the positive/negative token weights learnable parameters that are optimized jointly with the model, whereas FIGA and other baselines fix them heuristically; (3) it adds a representation-level contrastive margin loss between semantically improved and degraded token embeddings, again without invoking RL or an external reward model.  None of the related papers (FIGA, discoPOP/LRML, DPO, AtS) combine semantic token identification with weighted CE + contrastive regularization.  Hence the method is a non-trivial extension rather than a replication of existing ideas, although each component (frozen encoder similarity, learnable weights, contrastive loss) has precedents in other sub-fields, so the overall novelty is moderate rather than radical.', 'novelty_score': 7, 'significance_reason': 'If the reported +1 GPT-4 reward and +16-point increase in recall of truly improved tokens generalise, SAFARI meaningfully closes the gap between cheap SFT-style tuning and costly RLHF/DPO, addressing a practical bottleneck for many labs that cannot run RL.  By requiring only a frozen sentence encoder it keeps compute and data demands low, so the technique could be widely adopted in industry fine-tuning pipelines.  Academically, it provides a concrete, quantitatively evaluated answer to an acknowledged limitation of FIGA, and its contrastive term opens a path to study representation-level alignment signals.  However, the improvements are incremental (≈+1 reward point, 57 % vs 50 % human win-rate) and the reliance on a separate encoder slightly complicates training, so the societal/academic impact, while meaningful, is not transformational.', 'significance_score': 7}
